spring:
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: jdbc:mysql://localhost:3306/travel
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
